# `execution` 模块的 `AGENT` 规则

执行前必读：

1. `agents.md`
2. `input-output.md`
3. `handoff.md`
4. `dispatch.md`
5. 涉及团队运行时，再读 `team-runtime.md` 和 `team-recovery.md`

工作方式：

- 先明确当前自己处于哪个阶段角色
- 再明确当前输入是否完整
- 输出必须写回正式文件，不以聊天内容代替

禁止事项：

- 不明确输入就直接开始做实现
- 把未验证的内容写成已完成
- 把长篇推理链直接传给下游
- 一次性接收多个边界不清的任务包
