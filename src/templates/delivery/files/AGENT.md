# `delivery` 模块的 `AGENT` 规则

涉及 Git 提交、推送或交付时：

1. 先读 `branch-policy.md`
2. 再读 `commit-policy.md`
3. 再读 `push-policy.md`
4. 推送前按 `delivery-checklist.md` 自检
5. 如果当前处于某条 `change` 下，优先补当前 change 目录中的 `commit-message.md` 与 `delivery-checklist.md`

禁止事项：

- 不写“已完成”但不给验证证据
- 不跳过 commit 信息
- 不跳过 push 前自检
