# 运行时 Playbook：调试闭环

## 最小执行步骤
1. 先固定失败现象与最小复现步骤，再开始定位根因。
2. 把失败日志、根因假设和修复方案登记到 `.nfc`，并准备写回 `decisions.md / status.md`。
3. 修复后必须重跑对应验证并记录剩余风险。

## 共通约束
- 正式真相源始终是 `.specnfc/` 与 `specs/`。
- playbook 只能指导执行，不得取代阶段门禁。
- 需要写回时必须更新 `.nfc/sync/*`。
